# 開發者文檔 - 升中面試訓練係統

## 項目概览

这是一个为香港小學生设計的本地Web应用，用于升中面試的係統化訓練。係統提供AI驱動的訓練計劃生成、題目練習、即時反馈、進度追踪和弱點分析等功能。

**技術栈**：
- 前端：React 18 + TypeScript + Vite + Ant Design 5.x
- 後端：Node.js + Express + TypeScript
- 數據庫：MySQL 8.0+
- AI服務：DeepSeek API
- 狀態管理：Zustand
- HTTP客户端：Axios

**核心特點**：
- 🏠 本地运行，保护隐私
- 🤖 AI驱動的个性化訓練
- 📊 全面的進度追踪和弱點分析
- 🎯 針對TOP學校的定制化內容

---

## 係統架构

### 整体架构图

```
┌─────────────────────────────────────┐
│       浏览器 (localhost:5173)        │
│                                     │
│  ┌─────────────────────────────┐   │
│  │     React Application       │   │
│  │  - Dashboard (仪表盘)        │   │
│  │  - Practice (練習界面)       │   │
│  │  - Feedback (反馈查看)       │   │
│  │  - Progress (進度报告)       │   │
│  │  - Settings (係統设置)       │   │
│  │  - Questions (題庫管理)      │   │
│  │  - Schools (學校檔案)        │   │
│  │  - Interview Memory (回憶)  │   │
│  └─────────────────────────────┘   │
└──────────────┬──────────────────────┘
               │ HTTP REST API
               │ (Axios)
┌──────────────┴──────────────────────┐
│    Express Server (localhost:3001)  │
│                                     │
│  ┌─────────────────────────────┐   │
│  │      API Routes             │   │
│  │  /api/schools               │   │
│  │  /api/questions             │   │
│  │  /api/plans                 │   │
│  │  /api/sessions              │   │
│  │  /api/feedback              │   │
│  │  /api/progress              │   │
│  │  /api/ai                    │   │
│  │  /api/weaknesses            │   │
│  │  /api/data                  │   │
│  │  /api/settings              │   │
│  └─────────────────────────────┘   │
│                                     │
│  ┌─────────────────────────────┐   │
│  │    Business Logic           │   │
│  │  - AI Service Integration   │   │
│  │  - Data Processing          │   │
│  │  - Error Handling           │   │
│  └─────────────────────────────┘   │
└──────────────┬──────────────────────┘
               │
               ├─────────────> MySQL (localhost:3306)
               │                - training_plans
               │                - daily_tasks
               │                - sessions
               │                - qa_records
               │                - feedback
               │                - questions
               │                - school_profiles
               │                - student_weaknesses
               │
               └─────────────> DeepSeek API
                                (https://api.deepseek.com)
                                - 訓練計劃生成
                                - 題目生成
                                - 反馈分析
                                - 學校檔案生成
                                - 弱點分析
```

---

## 數據庫设計

### ER 图

```
┌─────────────────┐
│ school_profiles │
│─────────────────│
│ id (PK)         │
│ code (UNIQUE)   │◄────┐
│ name            │     │
│ focus_areas     │     │ FK
│ ...             │     │
└─────────────────┘     │
                        │
┌─────────────────┐     │
│   questions     │     │
│─────────────────│     │
│ id (PK)         │     │
│ category        │     │
│ question_text   │     │
│ difficulty      │     │
│ school_code     │─────┘
│ source          │
│ ...             │
└───────┬─────────┘
        │
        │ FK (question_id)
        │
┌───────┴─────────┐
│   qa_records    │
│─────────────────│
│ id (PK)         │
│ session_id (FK) │────┐
│ question_id (FK)│    │
│ answer_text     │    │
│ ...             │    │
└─────────────────┘    │
                       │
┌─────────────────┐    │
│    sessions     │    │
│─────────────────│    │
│ id (PK)         │◄───┘
│ plan_id (FK)    │────┐
│ category        │    │
│ mode            │    │
│ ...             │    │
└─────────────────┘    │
                       │
┌─────────────────┐    │
│ training_plans  │    │
│─────────────────│    │
│ id (PK)         │◄───┘
│ student_name    │
│ target_school   │
│ start_date      │
│ category_alloc  │
│ ...             │
└────────┬────────┘
         │
         │ FK (plan_id)
         │
┌────────┴────────┐
│  daily_tasks    │
│─────────────────│
│ id (PK)         │
│ plan_id (FK)    │
│ date            │
│ category        │
│ ...             │
└─────────────────┘

┌─────────────────┐
│    feedback     │
│─────────────────│
│ id (PK)         │
│ session_id (FK) │
│ overall_score   │
│ feedback_json   │
│ ...             │
└─────────────────┘

┌───────────────────┐
│ student_weaknesses│
│───────────────────│
│ id (PK)           │
│ student_name      │
│ category          │
│ weakness_type     │
│ description       │
│ severity          │
│ practice_count    │
│ status            │
│ ...               │
└───────────────────┘
```

### 核心表說明

#### 1. `school_profiles` - 學校檔案
存储TOP學校的面試特點和評分標準。

**關键字段**：
- `code`: 學校代碼（如 SPCC, QC）
- `focus_areas`: JSON數組，面試重點領域
- `interview_style`: 面試風格描述

#### 2. `questions` - 題庫
存储所有練習題目，支持多來源。

**關键字段**：
- `category`: 專項類別（7種）
- `source`: 來源（seed, ai_generated, interview_memory, ai_generated_targeted）
- `school_code`: 關聯學校（可選）

#### 3. `training_plans` - 訓練計劃
AI生成或手動創建的訓練計劃。

**關键字段**：
- `category_allocation`: JSON對象，各類別的時間分配比例
- `ai_suggestions`: AI生成的訓練建議

#### 4. `sessions` - 練習會話
每次練習的會話記錄。

**關键字段**：
- `mode`: 練習模式（focus, mixed, mock）
- `status`: 會話狀態（in_progress, completed, paused）

#### 5. `feedback` - AI反馈
AI對練習會話的详细反馈。

**關键字段**：
- `overall_score`: 總体評分（0-100）
- `feedback_json`: JSON對象，详细反馈內容

#### 6. `student_weaknesses` - 學生弱點分析 🆕
從面試回憶或反馈中識別的學生弱點。

**關键字段**：
- `weakness_type`: 弱點類型（6種）
- `severity`: 嚴重程度（low, medium, high）
- `practice_count`: 已針對性練習次數
- `status`: 狀態（active, improved, resolved）

---

## API 接口设計

### 基础規范

**Base URL**: `http://localhost:3001/api`

**响应格式**：
```json
{
  "success": true|false,
  "message": "操作說明",
  "data": { ... }
}
```

**错误响应**：
```json
{
  "success": false,
  "message": "错误描述",
  "code": "ERROR_CODE",
  "statusCode": 400
}
```

### 主要API模块

#### 1. Schools API (`/api/schools`)
- `GET /` - 获取學校列表
- `GET /:id` - 获取學校详情
- `POST /` - 創建學校檔案
- `PUT /:id` - 更新學校檔案
- `DELETE /:id` - 删除學校檔案

#### 2. Questions API (`/api/questions`)
- `GET /` - 获取題目列表（支持筛選）
- `GET /:id` - 获取題目详情
- `POST /` - 創建題目
- `PUT /:id` - 更新題目
- `DELETE /:id` - 删除題目

#### 3. AI API (`/api/ai`)
- `POST /generate-plan` - AI生成訓練計劃
- `POST /generate-questions` - AI生成題目
- `POST /analyze-feedback` - AI分析反馈
- `POST /generate-school-profile` - AI生成學校檔案
- `POST /extract-interview-memory` - AI提取面試回憶
- `POST /save-interview-questions` - 保存面試題目
- `POST /save-weaknesses` - 保存弱點分析 🆕
- `POST /generate-questions-from-weaknesses` - 根據弱點生成題目 🆕
- `POST /test-connection` - 测試API连接

#### 4. Weaknesses API (`/api/weaknesses`) 🆕
- `GET /` - 获取弱點列表
- `GET /:id` - 获取弱點详情
- `PATCH /:id/status` - 更新弱點狀態
- `DELETE /:id` - 删除弱點記錄
- `GET /stats/summary` - 弱點統計
- `GET /stats/trends` - 弱點趋勢分析

---

## 前端架构

### 目錄結构

```
frontend/
├── src/
│   ├── pages/              # 页面組件
│   │   ├── Dashboard/      # 仪表盘
│   │   ├── Practice/       # 練習界面
│   │   ├── Feedback/       # 反馈查看
│   │   ├── Progress/       # 進度报告
│   │   ├── Settings/       # 係統设置
│   │   ├── Questions/      # 題庫管理
│   │   ├── Schools/        # 學校檔案
│   │   ├── InterviewMemory/# 面試回憶
│   │   └── TrainingPlan/   # 訓練計劃
│   ├── components/         # 通用組件
│   ├── store/              # Zustand狀態管理
│   ├── utils/              # 工具函數
│   ├── App.tsx             # 应用入口
│   └── main.tsx            # React入口
├── public/                 # 静態資源
└── package.json
```

### 路由結构

```typescript
const routes = [
  { path: '/', element: <Dashboard /> },
  { path: '/training-plan', element: <TrainingPlan /> },
  { path: '/practice', element: <Practice /> },
  { path: '/feedback', element: <Feedback /> },
  { path: '/progress', element: <Progress /> },
  { path: '/questions', element: <Questions /> },
  { path: '/schools', element: <Schools /> },
  { path: '/interview-memory', element: <InterviewMemory /> },
  { path: '/settings', element: <Settings /> },
];
```

### 狀態管理（Zustand）

```typescript
// Example: settingsStore
interface SettingsState {
  studentName: string;
  targetSchool: string;
  dailyDuration: number;
  apiKey: string;
  setStudentName: (name: string) => void;
  setTargetSchool: (school: string) => void;
  // ...
}
```

---

## AI集成說明

### DeepSeek API集成

**配置位置**: `backend/.env`

```env
DEEPSEEK_API_KEY=sk-...
DEEPSEEK_API_URL=https://api.deepseek.com
```

**客户端实现**: `backend/src/ai/deepseek.ts`

```typescript
class DeepSeekClient {
  async chat(messages: ChatMessage[]): Promise<string> {
    // 調用API并返回响应
  }
}
```

### AI功能清单

1. **訓練計劃生成**
   - 输入：學生信息、目標學校、時間范围
   - 输出：每日任務安排、類別分配建議

2. **題目生成**
   - 输入：類別、難度、數量、學校代碼
   - 输出：符合要求的題目列表

3. **反馈分析**
   - 输入：問題和學生回答
   - 输出：評分、優缺點、改進建議

4. **學校檔案生成**
   - 输入：學校基本信息
   - 输出：面試特點、重點領域、建議

5. **面試回憶分析** 🆕
   - 输入：面試回憶文本
   - 输出：提取的題目列表 + 學生弱點分析

6. **針對性題目生成** 🆕
   - 输入：弱點ID列表或類別
   - 输出：針對弱點的練習題目

---

## 核心业務流程

### 1. 創建訓練計劃流程

```
用户输入信息
    ↓
調用 POST /api/ai/generate-plan
    ↓
AI生成計劃和任務
    ↓
保存到 training_plans + daily_tasks
    ↓
返回計劃详情
```

### 2. 練習流程

```
選擇類別和模式
    ↓
創建會話 (POST /api/sessions)
    ↓
获取題目列表
    ↓
用户作答
    ↓
提交答案 (POST /api/sessions/:id/answer)
    ↓
結束會話
    ↓
请求AI反馈 (POST /api/ai/analyze-feedback)
    ↓
保存反馈 (feedback表)
```

### 3. 面試回憶分析流程 🆕

```
用户输入面試回憶文本
    ↓
調用 POST /api/ai/extract-interview-memory
    ↓
AI提取：
  - 面試題目列表
  - 學生弱點分析
    ↓
前端展示提取結果
    ↓
用户確认并保存
    ↓
同時保存到：
  - questions表 (題目)
  - student_weaknesses表 (弱點)
```

### 4. 針對性題目生成流程 🆕

```
查看弱點列表
    ↓
選擇要加强的弱點
    ↓
調用 POST /api/ai/generate-questions-from-weaknesses
    ↓
AI分析弱點特征
    ↓
生成針對性題目
    ↓
保存到 questions表 (source='ai_generated_targeted')
    ↓
更新弱點的 practice_count
```

---

## 開發指南

### 环境要求

- Node.js >= 18.0.0
- MySQL >= 8.0
- npm >= 9.0.0

### 快速開始

```bash
# 1. 克隆項目
cd /path/to/interview-training-system

# 2. 配置环境变量
cp backend/.env.example backend/.env
# 编輯 backend/.env，填入 DEEPSEEK_API_KEY

# 3. 安装依赖
npm run install:all

# 4. 初始化數據庫
npm run db:init

# 5. 启動開發服務器
./dev.sh
# 或使用 npm run dev
```

### 添加新功能

#### 1. 添加新的API端點

```typescript
// backend/src/routes/myroute.ts
import { Router } from 'express';

const router = Router();

router.get('/my-endpoint', async (req, res) => {
  // 实现邏輯
  res.json({ success: true, data: {} });
});

export default router;
```

```typescript
// backend/src/index.ts
import myRoutes from './routes/myroute.js';
app.use('/api/my', myRoutes);
```

#### 2. 添加新的前端页面

```tsx
// frontend/src/pages/MyPage/index.tsx
export default function MyPage() {
  return <div>My New Page</div>;
}
```

```tsx
// frontend/src/App.tsx
import MyPage from './pages/MyPage';

const routes = [
  // ...
  { path: '/my-page', element: <MyPage /> },
];
```

#### 3. 添加新的數據表

```sql
-- backend/src/db/schema.sql
CREATE TABLE IF NOT EXISTS my_table (
  id INT AUTO_INCREMENT PRIMARY KEY,
  name VARCHAR(100) NOT NULL,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

重新运行數據庫初始化：
```bash
npm run db:init
```

### 調試技巧

#### 1. 後端調試

```bash
# 查看後端日志
tail -f /tmp/backend.log

# 或在终端直接运行
cd backend && npm run dev
```

#### 2. 前端調試

浏览器開發者工具 → Console/Network

#### 3. 數據庫調試

```bash
mysql -u root -p
use interview_training;
SELECT * FROM training_plans;
```

#### 4. API测試

```bash
# 使用 curl
curl http://localhost:3001/api/schools

# 或使用 Postman / Insomnia
```

---

## 测試

### 单元测試（待完善）

```bash
# 後端测試
cd backend && npm test

# 前端测試
cd frontend && npm test
```

### 手動测試清单

- [ ] 創建訓練計劃
- [ ] 開始練習會話
- [ ] 提交答案并获取反馈
- [ ] 查看進度报告
- [ ] 管理題庫（增删改查）
- [ ] 面試回憶錄入和分析
- [ ] 根據弱點生成題目
- [ ] 導出/導入數據

---

## 部署

### 本地部署（推荐）

```bash
# 一键启動
./dev.sh

# 或使用 quick-start
./quick-start.sh
```

### 生产环境部署（可選）

1. 构建前端：
```bash
cd frontend && npm run build
```

2. 配置後端服務静態文件：
```typescript
// backend/src/index.ts
app.use(express.static('../frontend/dist'));
```

3. 使用 PM2 管理進程：
```bash
npm install -g pm2
pm2 start backend/src/index.ts --name interview-training
```

---

## 故障排查

### 常见問題

#### 1. 後端无法启動

**症狀**：端口 3001 无响应

**解决**：
```bash
# 检查進程
lsof -i :3001

# 清理残留進程
pkill -9 -f "tsx watch"

# 重新启動
cd backend && npm run dev
```

#### 2. 數據庫连接失敗

**症狀**：`ECONNREFUSED` 错误

**解决**：
```bash
# 检查 MySQL 是否运行
mysql.server status

# 启動 MySQL
mysql.server start

# 检查 backend/.env 配置
```

#### 3. AI API 調用失敗

**症狀**：AI 功能返回错误

**解决**：
- 检查 `DEEPSEEK_API_KEY` 是否配置
- 使用测試连接功能：Settings → 测試连接
- 检查网络连接

#### 4. 前端页面空白

**症狀**：浏览器显示空白页

**解决**：
- 打開浏览器控制台查看错误
- 检查前端是否成功启動（5173端口）
- 清除浏览器缓存

---

## 性能優化

> **重要**：详细的性能優化文檔请參考 [`docs/PERFORMANCE.md`](./docs/PERFORMANCE.md)

### 性能要求

- **前端页面首次加载時間**：< 1 秒（本地环境）
- **API 响应時間**：< 500ms（简单查询），< 2 秒（复杂查询）
- **页面刷新**：所有页面刷新後能正常加载數據

### 1. 數據庫優化

- ✅ **索引優化**：为所有常用查询字段添加索引（category, difficulty, school_code, status 等）
- ✅ **查询缓存**：對频繁访問的查询結果進行缓存（學校列表、統計信息，5 分鐘 TTL）
- ✅ **慢查询监控**：記錄超過 100ms 的查询，帮助識別性能瓶颈
- ✅ **JSON 字段統一解析**：在數據庫访問层統一解析，避免重复解析
- ✅ **分页查询**：使用 `queryWithPagination` 函數優化分页查询

### 2. 前端優化

- ✅ **API 请求優化**：
  - 并行请求：使用 `Promise.all` 并行加载數據
  - 请求去重：相同请求在 pending 時不重复發送
  - 请求缓存：GET 请求缓存 5 分鐘
  - 请求取消：組件卸载時自動取消未完成的请求
  - 错误重試：网络错误時自動重試 1 次（指數退避）
- ✅ **React 性能優化**：
  - 使用 `useMemo` 缓存計算結果
  - 使用 `useCallback` 缓存回調函數
  - 使用 `React.memo` 優化列表組件
  - 優化 columns 定义，避免每次渲染重新創建
- ✅ **页面優化**：
  - Dashboard 页面：并行加载任務、會話、弱點數據
  - Questions 页面：并行加载題目、統計、學校列表

### 3. API優化

- ✅ **请求缓存**：GET 请求自動缓存 5 分鐘
- ✅ **批量API調用**：使用 `Promise.all` 并行执行多个请求
- ✅ **查询結果缓存**：後端對频繁访問的數據使用缓存
- ✅ **超時设置**：
  - **非 AI 接口**：默认 10 秒超時（快速响应）
  - **AI 接口**：不设置超時限制（`timeout: 0`），允许長時間处理
  - **详细規范**：參见 [`docs/DEVELOPMENT_GUIDE.md`](./docs/DEVELOPMENT_GUIDE.md#api-超時设置規范)

### 性能监控

- **前端**：使用浏览器開發者工具 Network 面板和 React DevTools Profiler
- **後端**：慢查询日志（> 100ms）自動输出到控制台

详细內容请參考 [`docs/PERFORMANCE.md`](./docs/PERFORMANCE.md)

---

## 安全考虑

### 1. API Key 管理

- DeepSeek API Key 存储在本地 `.env` 文件
- 前端显示時進行脱敏处理（sk-...）
- 從不提交 `.env` 文件到版本控制

### 2. 數據隐私

- 所有數據存储在本地 MySQL
- 不上傳學生數據到云端
- 只有AI分析時才發送必要數據到DeepSeek

### 3. 输入验证

- 所有API端點進行參數验证
- 使用 AppError 統一错误处理
- SQL查询使用參數化防止注入

---

## 贡献指南

### 代碼風格

- 使用 TypeScript 進行類型检查
- 遵循 ESLint 配置
- 使用 Prettier 格式化代碼

### 提交規范

```
feat: 新功能
fix: 修复bug
docs: 文檔更新
refactor: 代碼重构
test: 测試相關
chore: 构建/工具相關
```

### Pull Request 流程

1. Fork 項目
2. 創建功能分支
3. 编写代碼和测試
4. 提交 PR 并說明变更

---

## 附錄

### A. 專項類別說明

1. **english-oral** - 英文口語：自我介绍、話題討論、情景對話
2. **chinese-oral** - 中文口語：朗读、讲故事、看图說話
3. **logic-thinking** - 邏輯思維：數學推理、图形規律、脑筋急转弯
4. **current-affairs** - 時事常識：新闻、社會議題、政治基础
5. **science-knowledge** - 科學常識：自然、科技、环保
6. **personal-growth** - 个人成長：兴趣爱好、目標規劃、品格
7. **group-discussion** - 小組討論：团队合作、辩論、角色扮演

### B. 弱點類型說明

1. **vocabulary** - 詞汇量不足
2. **grammar** - 語法错误
3. **logic** - 邏輯不清晰
4. **knowledge_gap** - 知識盲区
5. **confidence** - 信心不足
6. **expression** - 表達能力弱

### C. 相關資源

- [DeepSeek API 文檔](https://platform.deepseek.com/docs)
- [React 官方文檔](https://react.dev/)
- [Ant Design 組件庫](https://ant.design/)
- [Express 文檔](https://expressjs.com/)

---

**版本**: v1.0.0
**最後更新**: 2026-01-24
**維护者**: 開發团队
